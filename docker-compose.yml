services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/data
    env_file:
      - .env
    environment:
      - DEBUG=True
    restart: unless-stopped

volumes:
  sqlite_data:
